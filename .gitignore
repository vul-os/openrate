# Go
/openrate
*.test
*.out

# Node / Vite
web/node_modules/
web/.vite/

# Playwright (E2E boot guard) output
web/test-results/
web/playwright-report/
web/blob-report/
web/.playwright/

# web/dist is committed (the built UI is embedded into the binary so a clone is
# self-contained); regenerate with `npm --prefix web run build`.

# Local
.env
*.log
.DS_Store
