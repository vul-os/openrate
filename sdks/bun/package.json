{
  "name": "@vul-os/openrate-bun",
  "version": "0.1.2",
  "description": "openrate for Bun — the C ABI in-process, or the sidecar over loopback.",
  "type": "module",
  "module": "index.ts",
  "exports": { ".": "./index.ts" },
  "license": "MIT OR Apache-2.0",
  "keywords": ["currency", "exchange-rate", "fx", "bun", "ffi"],
  "files": ["index.ts", "README.md"],
  "engines": { "bun": ">=1.1.0" },
  "scripts": {
    "check": "../node/node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "example:direct": "bun run examples/direct.ts",
    "example:sidecar": "bun run examples/sidecar.ts"
  },
  "devDependencies": { "@types/bun": "1.3.14" }
}
